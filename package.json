{
  "name": "cityhub",
  "version": "0.1.0",
  "private": true,
  "description": "CityHUB — TMA city concierge",
  "scripts": {
    "dev": "python3 -m http.server 8765",
    "dev:worker": "npx wrangler dev worker/index.js --port 8787",
    "deploy:worker": "npx wrangler deploy",
    "deploy:pages": "npx wrangler pages deploy . --project-name cityhub"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
